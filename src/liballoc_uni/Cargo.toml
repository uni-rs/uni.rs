[package]

name = "alloc_uni"
version = "0.0.1"
authors = ["Baptiste Covolato"]

[dependencies]

core = { path = "../rust-libs/core" }
spin = { path = "../deps/spinlock-rs" }
heap = { path = "../libheap", features = [ "with-core" ] }
