[package]

name = "heap"
version = "0.0.1"
authors = ["Baptiste Covolato"]

[lib]
name = "heap"

[features]

with-core = [ "core" ]

[dependencies]

core = { path = "../rust-libs/core", optional = true }
