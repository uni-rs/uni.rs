initSidebarItems({"struct":[["Arc","An atomically reference counted wrapper for shared state.ExamplesIn this example, a large vector is shared between several threads. With simple pipes, without `Arc`, a copy would have to be made for each thread.When you clone an `Arc<T>`, it will create another pointer to the data and increase the reference counter."],["Weak","A weak pointer to an `Arc`.Weak pointers will not keep the data inside of the `Arc` alive, and can be used to break cycles between `Arc` pointers."]]});